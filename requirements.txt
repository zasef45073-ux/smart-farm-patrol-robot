# ─────────────────────────────────────────────────────────────────────
# 스마트 축사 자율순찰 — Python 의존성 (시스템 python3 / ROS 2 Humble 측)
# ─────────────────────────────────────────────────────────────────────
# ⚠️ numpy 는 반드시 1.x(<2). ROS Humble 의 cv_bridge / 시스템 matplotlib 가
#    numpy 1.x ABI 로 빌드돼 있어, numpy 2.x 면 yolo_view 등이 깨진다.
numpy<2
opencv-python<4.11        # numpy<2 호환 (cv_bridge 와 동시 사용)
ultralytics>=8.4          # YOLO-Pose (소 검출 + 14키포인트 + 파행)
torch                     # ultralytics 백엔드
matplotlib>=3.8           # numpy2 비호환 시스템 mpl 대체용
PyYAML                    # 설정/웨이포인트 파싱

# ── pip 아님(별도 설치) ───────────────────────────────────────────────
# [ROS 2 Humble · apt]
#   sudo apt install ros-humble-desktop ros-humble-navigation2 \
#       ros-humble-nav2-bringup ros-humble-slam-toolbox \
#       ros-humble-twist-mux ros-humble-cv-bridge
#
# [Isaac Sim 5.1 + Isaac Lab · 별도 venv ~/dev_ws/venv/isaaclab]
#   IsaacLab 공식 설치 참조 (isaaclab.sh -p 로 isaac/*.py 실행)
#
# [웹 대시보드]  →  dashboard/requirements.txt (fastapi, uvicorn, paho-mqtt 등)
